import 'package:flutter/material.dart';

class JKBExpertPage extends StatelessWidget {
  static const String routeName = "/expert";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("专家"),
      ),
      body: Text("专家"),
    );
  }
}
