import 'package:flutter/material.dart';

class JKBHealthPage extends StatelessWidget {
  static const String routeName = "/health";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("健康"),
      ),
      body: Text("健康"),
    );
  }
}
